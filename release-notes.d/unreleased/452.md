---
pr: 452
url: https://github.com/llm-d/llm-d-async/pull/452
author: wseaton
date: 2026-09-17
---
Adds a `sql` transport backed by Postgres. Dispatchers lease hash partitions of each queue and batch dispatch, acks, and cancellation checks, and each request's envelope and payload are stored in separate columns. A `sql-quota` gate enforces `redis-quota` limits exactly across processors from the same database, so the sql transport needs no Redis.
