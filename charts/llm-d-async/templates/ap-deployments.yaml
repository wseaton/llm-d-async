{{- /* ---- Fail-fast configuration validation ----
       Turn common misconfigurations into clear install-time errors instead of
       runtime crash-loops or silently dropped requests. */ -}}
{{- if not (include "llm-d-async.transport" .) }}
{{- fail "No transport configured: set ap.transport (redis-pubsub|redis-sortedset|gcp-pubsub|sql) with ap.transportConfig, or enable a backend via ap.redis.enabled / ap.gcpPubSub.enabled (deprecated)." }}
{{- end }}
{{- if .Values.ap.transport }}
{{- /* New transport surface. */ -}}
{{- if not .Values.ap.transportConfig }}
{{- fail "ap.transport is set but ap.transportConfig is empty: provide the transport configuration document (see the README 'Transport Configuration' section)." }}
{{- end }}
{{- if and (hasPrefix "redis" .Values.ap.transport) (not (hasKey .Values.ap.transportConfig "url")) (not (include "llm-d-async.redisSecretName" .)) }}
{{- fail "redis transport has no connection: set ap.transportConfig.urlSecret.url (chart creates a Secret) or ap.transportConfig.urlSecret.name/key (reference an existing Secret), or an inline url in ap.transportConfig (dev only). The deprecated ap.redis.url / ap.redis.secretName still work. The resolved Secret is injected as REDIS_URL." }}
{{- end }}
{{- if and (eq .Values.ap.transport "sql") (not (hasKey .Values.ap.transportConfig "url")) (not (include "llm-d-async.redisSecretName" .)) }}
{{- fail "sql transport has no connection: set ap.transportConfig.urlSecret.url (chart creates a Secret) or ap.transportConfig.urlSecret.name/key (reference an existing Secret), or an inline url in ap.transportConfig (dev only). The resolved Secret is injected as SQL_URL." }}
{{- end }}
{{- else }}
{{- /* Deprecated per-backend surface. */ -}}
{{- if .Values.ap.redis.enabled }}
{{- if and (not .Values.ap.redis.url) (not .Values.ap.redis.secretName) }}
{{- fail "ap.redis is enabled but no connection is configured: set ap.redis.url, or set ap.redis.secretName/ap.redis.secretKey to reference an existing Secret." }}
{{- end }}
{{- else if .Values.ap.gcpPubSub.enabled }}
{{- if not .Values.ap.gcpPubSub.projectId }}
{{- fail "ap.gcpPubSub is enabled but ap.gcpPubSub.projectId is empty: the GCP Pub/Sub backend requires a project ID (the process panics on startup without it)." }}
{{- end }}
{{- end }}
{{- if not .Values.ap.igwBaseURL }}
{{- if and .Values.ap.redis.enabled (not .Values.ap.redis.queuesConfig) }}
{{- fail "ap.igwBaseURL is required: set it to your inference gateway base URL, or provide per-queue igw_base_url entries via ap.redis.queuesConfig." }}
{{- else if and .Values.ap.gcpPubSub.enabled (not .Values.ap.gcpPubSub.topicsConfig) }}
{{- fail "ap.igwBaseURL is required: set it to your inference gateway base URL, or provide per-topic igw_base_url entries via ap.gcpPubSub.topicsConfig." }}
{{- end }}
{{- end }}
{{- if and .Values.ap.redis.enabled (eq (.Values.ap.messageQueueImpl | default "redis-pubsub") "redis-sortedset") .Values.ap.redis.queuesConfig .Values.ap.redis.gateType }}
{{- fail "Cannot set both queuesConfig and gateType. Use queuesConfig with per-queue gate_type instead." }}
{{- end }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "llm-d-async.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "llm-d-async.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "llm-d-async.selectorLabels" . | nindent 6 }}
  replicas: 1
  template:
    metadata:
      labels:
        {{- include "llm-d-async.selectorLabels" . | nindent 8 }}
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - command:
        # Must match the binary produced by the Dockerfile (ENTRYPOINT /llm-d-async).
        - /llm-d-async
        args:
          {{- /* Unified transport surface. On the deprecated per-backend values the
                 config is synthesized from them (see _helpers.tpl), so the processor
                 never receives the retired --message-queue-impl / --redis.* / --pubsub.*
                 flags regardless of which values the operator sets. */}}
          - --transport={{ include "llm-d-async.transport" . }}
          - --transport-config
          - {{ include "llm-d-async.transportConfig" . | quote }}
          {{- if .Values.ap.prometheusURL }}
          - --prometheus-url={{ .Values.ap.prometheusURL }}
          {{- end }}
          {{- if .Values.ap.prometheusCacheTTL }}
          - --prometheus-cache-ttl={{ .Values.ap.prometheusCacheTTL }}
          {{- end }}
          {{- if .Values.ap.workerPools }}
          - --pool-config-file=/etc/llm-d-async/config/worker-pools.json
          {{- end }}
          {{- if .Values.ap.transformConfig }}
          - --transform-config-file=/etc/llm-d-async/config/transform-config.json
          {{- end }}
          {{- if .Values.ap.requestMergePolicyConfig }}
          - --request-merge-policy-config-file=/etc/llm-d-async/config/request-merge-policy.json
          {{- end }}
          - --concurrency={{ .Values.ap.concurrency | default 64 }}
          - --gate-wait-timeout={{ .Values.ap.gateWaitTimeout }}
          - --drain-timeout={{ .Values.ap.drainTimeout | default "2m" }}
          - --health-port={{ .Values.ap.health.port | default 8081 }}
          - --metrics-endpoint-auth={{ .Values.ap.metrics.secure }}
          - --metrics-port={{ .Values.ap.metrics.port | default 9090 }}
          {{- if .Values.ap.tls.insecureSkipVerify }}
          - --tls-insecure-skip-verify=true
          {{- end }}
          {{- if .Values.ap.tls.caCertKey }}
          - --tls-ca-cert=/etc/tls/{{ .Values.ap.tls.caCertKey }}
          {{- end }}
          {{- if and .Values.ap.tls.certKey .Values.ap.tls.keyKey }}
          - --tls-cert=/etc/tls/{{ .Values.ap.tls.certKey }}
          - --tls-key=/etc/tls/{{ .Values.ap.tls.keyKey }}
          {{- end }}
        image: "{{ .Values.ap.image.repository }}:{{ .Values.ap.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: "{{ .Values.ap.imagePullPolicy }}"
        env:
          - name: LOG_LEVEL
            value: {{ if .Values.ap.logging }}{{ .Values.ap.logging.level | default "info" | quote }}{{ else }}"info"{{ end }}
          {{- if .Values.ap.otel.endpoint }}
          - name: POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
          - name: OTEL_EXPORTER_OTLP_ENDPOINT
            value: {{ .Values.ap.otel.endpoint | quote }}
          - name: OTEL_EXPORTER_OTLP_INSECURE
            value: {{ .Values.ap.otel.insecure | quote }}
          - name: OTEL_TRACES_SAMPLER
            value: {{ .Values.ap.otel.sampler | quote }}
          - name: OTEL_TRACES_SAMPLER_ARG
            value: {{ .Values.ap.otel.samplerArg | quote }}
          - name: OTEL_SERVICE_NAME
            value: "llm-d-async"
          - name: OTEL_RESOURCE_ATTRIBUTES
            value: "k8s.namespace.name=$(NAMESPACE),k8s.pod.name=$(POD_NAME),service.version={{ .Chart.AppVersion }}"
          {{- end }}
          {{- if and (include "llm-d-async.urlEnvVar" .) (include "llm-d-async.redisSecretName" .) }}
          - name: {{ include "llm-d-async.urlEnvVar" . }}
            valueFrom:
              secretKeyRef:
                name: {{ include "llm-d-async.redisSecretName" . }}
                key: {{ include "llm-d-async.redisSecretKey" . }}
          {{- end }}
          {{- with .Values.ap.env }}
          {{- toYaml . | nindent 10 }}
          {{- end }}
        name: llm-d-async
        ports:
          - name: metrics
            containerPort: {{ .Values.ap.metrics.port | default 9090 }}
            protocol: TCP
          - name: health
            containerPort: {{ .Values.ap.health.port | default 8081 }}
            protocol: TCP
        startupProbe:
          httpGet:
            path: /healthz
            port: health
          initialDelaySeconds: {{ .Values.ap.health.startupProbe.initialDelaySeconds | default 0 }}
          periodSeconds: {{ .Values.ap.health.startupProbe.periodSeconds | default 2 }}
          timeoutSeconds: {{ .Values.ap.health.startupProbe.timeoutSeconds | default 3 }}
          failureThreshold: {{ .Values.ap.health.startupProbe.failureThreshold | default 30 }}
        livenessProbe:
          httpGet:
            path: /healthz
            port: health
          initialDelaySeconds: {{ .Values.ap.health.livenessProbe.initialDelaySeconds | default 5 }}
          periodSeconds: {{ .Values.ap.health.livenessProbe.periodSeconds | default 10 }}
          timeoutSeconds: {{ .Values.ap.health.livenessProbe.timeoutSeconds | default 3 }}
          failureThreshold: {{ .Values.ap.health.livenessProbe.failureThreshold | default 3 }}
        readinessProbe:
          httpGet:
            path: /readyz
            port: health
          initialDelaySeconds: {{ .Values.ap.health.readinessProbe.initialDelaySeconds | default 5 }}
          periodSeconds: {{ .Values.ap.health.readinessProbe.periodSeconds | default 5 }}
          timeoutSeconds: {{ .Values.ap.health.readinessProbe.timeoutSeconds | default 3 }}
          failureThreshold: {{ .Values.ap.health.readinessProbe.failureThreshold | default 3 }}
        {{- with .Values.ap.securityContext }}
        securityContext:
          {{- toYaml . | nindent 12 }}
        {{- end }}
        {{- with .Values.ap.resources }}
        resources:
          {{- toYaml . | nindent 12 }}
        {{- end }}
        {{- if or .Values.ap.workerPools .Values.ap.transformConfig .Values.ap.requestMergePolicyConfig .Values.ap.tls.secretName }}
        volumeMounts:
          {{- if or .Values.ap.workerPools .Values.ap.transformConfig .Values.ap.requestMergePolicyConfig }}
          - name: ap-config
            mountPath: /etc/llm-d-async/config
            readOnly: true
          {{- end }}
          {{- if .Values.ap.tls.secretName }}
          - name: tls-certs
            mountPath: /etc/tls
            readOnly: true
          {{- end }}
        {{- end }}
      serviceAccountName: {{ include "llm-d-async.fullname" . }}
      terminationGracePeriodSeconds: 130
      {{- if or .Values.ap.workerPools .Values.ap.transformConfig .Values.ap.requestMergePolicyConfig .Values.ap.tls.secretName }}
      volumes:
        {{- if or .Values.ap.workerPools .Values.ap.transformConfig .Values.ap.requestMergePolicyConfig }}
        - name: ap-config
          configMap:
            name: {{ include "llm-d-async.fullname" . }}-config
        {{- end }}
        {{- if .Values.ap.tls.secretName }}
        - name: tls-certs
          secret:
            secretName: {{ .Values.ap.tls.secretName }}
        {{- end }}
      {{- end }}
      {{- with .Values.ap.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
